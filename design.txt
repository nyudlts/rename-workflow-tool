goals:
1. copy a directory of files from acmbd to adoc
2. generate a bag for each cuid with all neccesary keys in bag-info.txt
3. transfer bag to rstar

commands to implement:

rename project init         generate a project directory
rename source transfer      copy files from acmbd to adoc host
rename sip validate         ensure all sip directory conforms to specification
rename aip process          this will generate bags in the aips directory for each cuid directory in sip
rename aip validate         bagit fast validation
rename aip transfer         transfer to rstar
rename project archive      closeout the project compress logs
